def solution(bridge_length, weight, truck_weights):
    time = 0
    if len(truck_weight) = 1 :
        return bridge_length + 1
    else :
        on = []
        for i in (len(bridge_length)-1) :
            on[i] = 0
        on[len(bridge_length)-1] = turck_weights.pop(0)
        
        for j in (len(truck_weights)) :
            if (sum(bridge_length)+truck_weights[i]) <= weight :
                bridge_length.append.(pop(truck_weights[i]))
        
            
        
        
    return time